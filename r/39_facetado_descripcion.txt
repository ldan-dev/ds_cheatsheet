### TEXTO PARA LA IMAGEN
N/A - Lleva gráfico

### TEXTO PARA LA PUBLICACION
`facet_wrap()` y `facet_grid()` permiten crear subgráficos por categorías para comparar patrones entre grupos de forma eficiente.

Si quieres probar este código y aprender más detalles, date una vuelta por nuestro repositorio: https://github.com/ldan-dev/ds_cheatsheet

#rstats #tidyverse #ggplot2 #datascience #statistics #programming #UG #DICIS #acm

### CODIGO
R
library(ggplot2)
p <- ggplot(mtcars, aes(x=hp, y=mpg)) + geom_point() + facet_wrap(~cyl) + labs(title='Facetado por cilindros')
print(p)
ggsave('grafico.png', plot = p)
